# KIMA EIS V3

آخر تحديث: 2026-08-31 (ADR-035 — R3-002 مؤكد على جهاز حقيقي)

## المحتويات
- `30_WEB_APP/` — تطبيق الويب الفعلي (PWA)، تم التحقق من صحته النحوية
  (`node --check`) واختبار محرك القرار (`decision.test.mjs`) قبل التسليم.
- `00_CONTROL/` — وثائق التحكم، سجل القرارات، حزم اختبار المتصفح.
  - `DECISION_LOG.md` و`V3_GAP_REGISTER.txt`: **لم يكونا موجودين في
    الحزم المرفوعة أصلًا** رغم أن باقي الوثائق تشير إليهما بكثافة. تم
    إنشاؤهما من جديد هنا بدءًا من ADR-035 فقط — راجع ملاحظة الأمانة في
    أعلى كل ملف قبل الاعتماد عليهما كسجل كامل.
- `.github/workflows/browser-tests.yml` — اختبار آلي كامل (Node-logic +
  headless Chromium) يعمل تلقائيًا عند كل رفع؛ نتائجه في تبويب Actions.

## الحالة الحالية بإيجاز
- R1-001/R1-002، R9، R12: مؤكدة على جهاز حقيقي سابقًا (ADR-020/021/022).
- **R3-002 (فلتر شريط السياق): مؤكد على جهاز حقيقي الآن (ADR-035،
  2026-08-31)** — انظر `00_CONTROL/DECISION_LOG.md`.
- بقية بنود Gate D (استيراد Excel، النسخ الاحتياطي، الوضع دون اتصال،
  إلخ): لم تُختبر بعد على هذا الجهاز — القائمة الكاملة والترتيب
  الموصى به في `00_CONTROL/PROJECT_STATE.md`، قسم "Next task".

## GitHub Pages
لتشغيل التطبيق على رابط حقيقي: Settings → Pages → Deploy from branch
→ `main` / `(root)`. `index.html` يجب أن يكون في جذر فرع النشر.

## الاختبار الآلي
راجع تبويب Actions لرؤية نتائج كل تشغيل. البند غير المؤتمت هو اختبار
جهاز أندرويد حقيقي (لمس، شاشة صغيرة) — يبقى يدويًا بطبيعته.
